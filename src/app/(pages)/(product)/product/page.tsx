import { AllProductPages } from "@/components/pages";
import Layout from "@/Layout/Layout";

const page: React.FC = () => {
  return (
    <Layout>
      <AllProductPages />;
    </Layout>
  );
};

export default page;
