import { PotsPages } from "@/components/pages";
import Layout from "@/Layout/Layout";

const page: React.FC = () => {
  return (
    <Layout>
      <PotsPages />
    </Layout>
  );
};

export default page;
