import { GrowingMediaPages } from "@/components/pages";
import Layout from "@/Layout/Layout";

const page: React.FC = () => {
  return (
    <Layout>
      <GrowingMediaPages />
    </Layout>
  );
};

export default page;
